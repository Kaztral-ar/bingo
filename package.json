{
  "name": "bingo",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build:functions": "cd functions && npm install",
    "eas-build-pre-install": "if [ ! -d android ] && [ -d app ]; then mkdir -p android && mv app android/app && mv build.gradle android/build.gradle && mv settings.gradle android/settings.gradle && mv gradle.properties android/gradle.properties; fi"
  },
  "dependencies": {
    "expo": "^57.0.19"
  }
}
